# Changelog

## [0.1.0] - 2026-08-15

首个发布版本。个人记账插件：记录收支、查账、月度汇总，面向 DSH 深度优化。

### 功能

- `tally_add`：记一笔（金额/收支类型/类别/备注/日期），金额校验、备注 50 字截断、日期自动回退今天；
- `tally_list`：查账（按月/类别/关键词过滤，日期倒序，默认 20 条）；
- `tally_stats`：月度汇总（支出/收入/结余、类别 Top5、日均支出），紧凑单块输出；
- `tally_remove`：删错账，**二次确认**防误删；
- 内置中文分类：餐饮/交通/购物/居住/娱乐/医疗/教育/人情/工资/其他（可自定义）；
- 金额 ¥ 中文格式化。

### 存储与架构

- 本地 JSON 存储：`$DSH_HOME/plugins/tally.json`（`storagePath` 可配置）；
- **原子写入**（临时文件 + rename）防崩溃损坏；
- **串行写入队列**：PTC 并行调用不丢账；
- 零运行时依赖（Node 20+ 内置能力）；
- 静态 schema（KV-cache 友好）、协作式超时、紧凑文本 render。

### 测试

- 14 个零依赖断言（增删查改/汇总/类别/持久化/并发写/金额校验/render）。
